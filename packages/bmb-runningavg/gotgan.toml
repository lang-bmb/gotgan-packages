[package]
name = "bmb-runningavg"
version = "0.1.0"
description = "Running/moving average computation over a sliding window"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
