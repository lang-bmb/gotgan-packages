[package]
name = "bmb-dedup"
version = "0.1.0"
description = "Deduplication utilities for sorted vecs"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
