[package]
name = "bmb-graph"
version = "0.1.0"
description = "Graph data structure using adjacency lists with BFS and path queries"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
