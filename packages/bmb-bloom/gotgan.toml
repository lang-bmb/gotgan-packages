[package]
name = "bmb-bloom"
version = "0.1.0"
description = "Bloom filter with multiple hash functions for probabilistic set membership"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
