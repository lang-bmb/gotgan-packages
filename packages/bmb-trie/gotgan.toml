[package]
name = "bmb-trie"
version = "0.1.0"
description = "Integer trie (decimal digit sequences 0-9) with insert, search, and prefix query"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
