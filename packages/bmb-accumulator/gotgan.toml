[package]
name = "bmb-accumulator"
version = "0.1.0"
description = "Online running statistics accumulator (count, sum, min, max, mean)"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
