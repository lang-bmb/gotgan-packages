[package]
name = "bmb-histogram"
version = "0.1.0"
description = "Histogram / frequency distribution over configurable bins"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
