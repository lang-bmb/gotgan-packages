[package]
name = "bmb-priority-queue"
version = "0.1.0"
description = "Min-heap priority queue storing (key, value) pairs"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
