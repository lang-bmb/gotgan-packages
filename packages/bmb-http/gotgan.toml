[package]
name = "bmb-http"
version = "0.1.0"
description = "HTTP client and server utilities for BMB"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
