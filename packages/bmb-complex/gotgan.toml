[package]
name = "bmb-complex"
version = "0.1.0"
description = "Complex number arithmetic using scaled integer pairs (a+bi scaled by 1000)"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
