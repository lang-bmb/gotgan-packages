[package]
name = "bmb-uuid"
version = "0.1.0"
description = "UUID v4-like generation using a linear congruential generator"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
