[package]
name = "bmb-median"
version = "0.1.0"
description = "Running median computation using two heaps (vec-based)"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
