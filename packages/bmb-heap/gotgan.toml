[package]
name = "bmb-heap"
version = "0.1.0"
description = "Binary heap (priority queue) for BMB"
authors = ["BMB Team"]
license = "MIT"
repository = "https://github.com/lang-bmb/gotgan-packages"

[dependencies]
